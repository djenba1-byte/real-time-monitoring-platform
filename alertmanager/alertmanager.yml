route:
  receiver: monitoring-app
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: monitoring-app
    webhook_configs:
      - url: http://app:5000/webhook/alertmanager
        send_resolved: true
